apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openfoundry.fullname" . }}-ontology-engine
  labels:
    {{- include "openfoundry.labels" . | nindent 4 }}
    app.kubernetes.io/component: ontology-engine
spec:
  replicas: {{ .Values.ontologyEngine.replicaCount }}
  selector:
    matchLabels:
      {{- include "openfoundry.selectorLabels" (dict "name" "ontology-engine" "release" .Release.Name) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openfoundry.selectorLabels" (dict "name" "ontology-engine" "release" .Release.Name) | nindent 8 }}
        app.kubernetes.io/component: ontology-engine
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: ontology-engine
          image: {{ include "openfoundry.image" (dict "global" .Values.image "svc" .Values.ontologyEngine.image "name" "engine") }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.ontologyEngine.port }}
              protocol: TCP
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: {{ .Values.ontologyEngine.port | quote }}
            - name: OTEL_SERVICE_NAME
              value: ontology-engine
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.storage.postgres.existingSecret }}
                  key: {{ .Values.storage.postgres.secretKeys.username }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.storage.postgres.existingSecret }}
                  key: {{ .Values.storage.postgres.secretKeys.password }}
            - name: POSTGRES_URL
              value: {{ include "openfoundry.postgresUrl" . | quote }}
          envFrom:
            - configMapRef:
                name: {{ include "openfoundry.fullname" . }}-config
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.ontologyEngine.resources | nindent 12 }}
