/**
 * Custom scalar type aliases used across the platform.
 */

/** ISO 8601 date-time string (e.g., "2026-02-06T14:30:00Z"). */
export type DateTime = string;

/** ISO 8601 duration string (e.g., "P30D", "PT1H30M"). */
export type Duration = string;
